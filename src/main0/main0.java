package main0;

import static basix_funx.basix_funx.prnt;
import _lsp._Server;
import _lsp._TxtDocSrv;
public class main0{
    private serv: _Server;
    public static void main (String[] args ) {
        this.serv = new _Server11 (this);
        prnt ("tst" + 0);
    }
}
